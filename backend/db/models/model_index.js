const { Categories } = require('./recipe_categories.model');
const { Recipe } = require('./recipe_list.model');
const { User } = require('./user.model');

module.exports = {
    Categories,
    Recipe,
    User
}
